{
  "name": "mackenzie",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A mapped, sourced, multi-vector history of every golf course I've played.",
  "scripts": {
    "dev": "npx serve .",
    "parse": "node scripts/parse-grint.mjs",
    "geocode": "node scripts/geocode.mjs",
    "osm": "node scripts/fetch-osm.mjs",
    "holes": "node scripts/fetch-holes.mjs",
    "build": "node scripts/build.mjs",
    "validate": "node scripts/validate.mjs",
    "all": "npm run parse && npm run geocode && npm run osm && npm run holes && npm run build && npm run validate"
  }
}
